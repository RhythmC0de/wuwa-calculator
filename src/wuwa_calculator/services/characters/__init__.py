"""Character identity helpers with no UI dependencies."""

from wuwa_calculator.services.characters.identity import (
    normalize_character_id,
    resolve_character_id,
)

__all__ = ["normalize_character_id", "resolve_character_id"]
